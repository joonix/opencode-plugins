name: publish

on:
  push:
    tags:
      - "*-v*"

permissions:
  contents: read

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Require a commit on main
        run: |
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "Release tags must point to a commit on main." >&2
            exit 1
          fi
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.21.0
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.2.22
      - name: Validate release tag
        id: release
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          metadata="$(node scripts/release-metadata.mjs "$RELEASE_TAG")"
          echo "$metadata" | jq .
          echo "directory=$(echo "$metadata" | jq -r .directory)" >> "$GITHUB_OUTPUT"
          echo "name=$(echo "$metadata" | jq -r .name)" >> "$GITHUB_OUTPUT"
          echo "version=$(echo "$metadata" | jq -r .version)" >> "$GITHUB_OUTPUT"
      - name: Install locked dependencies
        run: bun install --frozen-lockfile
      - name: Test
        run: make test
      - name: Create release tarball
        id: pack
        env:
          PACKAGE_DIRECTORY: ${{ steps.release.outputs.directory }}
        run: |
          mkdir release
          npm pack --json --pack-destination release "./$PACKAGE_DIRECTORY" > release/pack.json
          tarball="$(jq -r '.[0].filename' release/pack.json)"
          test -n "$tarball" && test -f "release/$tarball"
          echo "tarball=release/$tarball" >> "$GITHUB_OUTPUT"
          sha512sum "release/$tarball"
      - name: Publish to npm with provenance
        run: npm publish "${{ steps.pack.outputs.tarball }}" --access public
